<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codekiller.fcsaadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fcsa_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fcsa_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FCSAAdmin">
        <activity android:name=".EditActivity"></activity>
        <activity android:name=".EventActivity" />
        <activity android:name=".PhotosActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".EbookActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>